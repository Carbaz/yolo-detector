# YOLO objects detection application

This application is designed to detect objects in images using a custom-trained YOLO
(You Only Look Once) model.

It provides functionalities for training the model, running detections on images,
and managing the application through a command-line interface.
